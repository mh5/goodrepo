pipeline {
  agent any
  string(name: 'Greeting', defaultValue: 'Hello', description: 'How should Igreet the world?')
  stages {
    stage('Build') {
      steps {
        echo 'lol'
      }
    }
  }
}
